# Product

## Register

product

## Users

开发者和测试人员在 Android 模拟器或真机上使用这个 Demo 验证 Agent Runtime、终端工具、无障碍操作和跨 App 自动化。他们通常需要一边观察 Agent 的执行过程，一边判断是否确认、停止、重试或继续下一步。

## Product Purpose

这是一个 Android Agent SDK 的可交互验证宿主。它需要让用户像使用成熟的移动聊天应用一样发起任务、阅读回答、理解工具执行状态、管理本地会话，并在发生错误或高影响操作时做出明确决定。

成功标准是：用户无需阅读原始日志就能在几秒内判断 Agent 当前状态；主任务始终是继续对话；工具过程可追溯但不打断聊天；返回前台后会话、草稿和当前状态不会表现得像应用重启。

## Brand Personality

冷静、透明、可靠。界面应该像一个熟悉的生产力工具，而不是玩具 Demo 或终端日志查看器。系统状态可以明确，但不制造紧张感；高影响操作需要清晰确认，但不把权限解释铺满主聊天区。

## Anti-references

- 不做“AI 魔法”式的夸张动画、霓虹渐变或装饰性机器人形象。
- 不把完整 stdout/stderr、JSON 和工具参数默认铺成聊天正文。
- 不使用多套互相矛盾的按钮、气泡、状态颜色或输入方式。
- 不让权限弹窗、API 配置和调试信息抢过用户消息与最终回答的视觉优先级。
- 不把主界面设计成密集终端控制台。

## Design Principles

1. **Chat first**：聊天流是唯一主叙事，其他信息围绕当前任务分层出现。
2. **Progressive disclosure**：默认展示“正在做什么”，点击后再看工具、耗时、结果摘要和错误详情。
3. **Action clarity**：发送、停止、重试、确认和新建会话必须在视觉上互斥且可预测。
4. **State continuity**：本地会话、草稿和运行状态在 Activity 重建与前后台切换后保持可理解。
5. **Trust through evidence**：成功、失败、取消和权限状态都要有明确文字，不依赖颜色单独表达。
6. **Runtime humility**：界面不把共享 Android UID 的受控 Runtime 描述成安全沙箱。

## Accessibility & Inclusion

- 重要触控目标不小于 44dp；文本和图标不能只依赖颜色传达状态。
- 支持系统字体缩放、键盘遮挡规避、TalkBack 可读的 contentDescription 和状态文本。
- 默认减少装饰性动画；状态变化使用短促、可跳过的反馈。
- 无悬浮窗权限时主界面仍然完整可用，悬浮窗只作为可选的后台观察入口。
